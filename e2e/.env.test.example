# Copy this to .env.test and fill in values
# These are used by Playwright tests — NEVER use production credentials

DB_URI=mongodb://localhost:27018/policecad_test
POLICE_CAD_API_URL=http://localhost:8081
POLICE_CAD_API_TOKEN=Bearer test-token
NODE_ENV=test
PORT=8080

# Test user credentials (must exist in the test database)
TEST_USER_EMAIL=testuser@test.com
TEST_USER_PASSWORD=TestPass123!
