<div
  id="detailsModal"
  class="modal fade"
  tabindex="-1"
  role="dialog"
  aria-hidden="true"
>
  <div class="modal-dialog modal-lg">
    <div class="modal-content bg-dark text-white">
      <div class="modal-body">
        <!-- Header -->
        <div class="d-flex justify-content-between align-items-center mb-3">
          <a href="javascript:void(0)" onclick="goBack()">
            <i class="fa fa-chevron-left text-white"></i>
          </a>
          <h4 id="detailsTitle" class="m-0">Details</h4>
          <button
            type="button"
            class="btn btn-primary btn-md"
            data-dismiss="modal"
            aria-hidden="true"
          >
            X
          </button>
        </div>
        <hr style="width: 88%; max-width: 100rem; border-color: #444" />

        <!-- Loading Indicator -->
        <div id="detailsLoading" class="text-center">
          <div id="loadingStatuses" class="list-group"></div>
        </div>

        <!-- Content -->
        <div id="detailsContent" style="display: none">
          <!-- Image -->
          <div class="text-center mb-4">
            <img
              id="detailsImage"
              class="rounded-circle"
              style="width: 160px; height: 160px; border: 4px solid #000"
              alt="Profile"
            />
          </div>

          <!-- Details -->
          <div id="detailsInfo"></div>

          <!-- Linked Items (Civilian Only) -->
          <div id="linkedItems" class="mt-4"></div>

          <!-- Criminal History (Civilian Only) -->
          <div id="criminalHistory" class="mt-4"></div>

          <!-- Arrest Reports (Civilian Only) -->
          <div id="arrestReports" class="mt-4"></div>

          <!-- Action Buttons -->
          <div id="actionButtons" class="mt-4"></div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .details-item {
    padding: 0.75rem;
    border-bottom: 1px solid #444;
    cursor: pointer;
  }
  .details-item:hover {
    background-color: #3b82f6;
  }
  .badge-status {
    padding: 0.5rem 1rem;
    border-radius: 0.25rem;
    font-size: 1rem;
  }
  .badge-revoked {
    background-color: #dc3545;
    color: white;
  }
  .badge-expired {
    background-color: #fd7e14;
    color: white;
  }
  .badge-stolen {
    background-color: #dc3545;
    color: white;
    padding: 0.5rem 1rem;
    font-size: 1rem;
  }
  .text-gray {
    color: #adb5bd;
  }
  .loading-item {
    display: flex;
    align-items: center;
    padding: 0.5rem;
    background-color: #333;
    border-radius: 0.25rem;
    margin-bottom: 0.5rem;
  }
  .loading-item .spinner-border {
    width: 1rem;
    height: 1rem;
    margin-right: 0.5rem;
  }
  .loading-item .fa-check {
    color: #28a745;
    margin-right: 0.5rem;
  }
  .loading-item .fa-times {
    color: #dc3545;
    margin-right: 0.5rem;
  }
</style>
